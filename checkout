<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FurniCo - Checkout</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom styles to enhance Tailwind */
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f7fafc; /* Light gray background */
        }
        .form-input {
            @apply w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500;
        }
    </style>
</head>
<body>
    <!-- Header Section -->
    <header class="bg-white shadow-lg p-4 md:p-6 sticky top-0 z-50 rounded-b-lg">
        <div class="container mx-auto flex justify-between items-center flex-wrap">
            <!-- Logo/Site Title -->
            <a href="detail.html" class="text-2xl font-bold text-indigo-700 hover:text-indigo-900 transition duration-300">FurniCo</a>

            <!-- Navigation Links (Hidden on small screens, shown on medium and up) -->
            <nav class="hidden md:flex space-x-6">
                <a href="index.html" class="text-gray-700 hover:text-indigo-700 font-medium transition duration-300">Home</a>
                <a href="list.html" class="text-gray-700 hover:text-indigo-700 font-medium transition duration-300">Shop</a>
                <a href="#" class="text-gray-700 hover:text-indigo-700 font-medium transition duration-300">About Us</a>
                <a href="#" class="text-gray-700 hover:text-indigo-700 font-medium transition duration-300">Contact</a>
            </nav>

            <!-- Mobile Menu Button (Visible on small screens) -->
            <button class="md:hidden p-2 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500">
                <svg class="w-6 h-6 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
            </button>
        </div>
    </header>

    <!-- Checkout Section -->
    <section class="container mx-auto px-4 py-8 md:py-12 bg-white rounded-lg shadow-md my-4 md:my-8">
        <h1 class="text-3xl md:text-4xl font-bold text-gray-900 text-center mb-8">Checkout</h1>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Shipping Information -->
            <div class="lg:col-span-2 bg-gray-50 p-6 rounded-lg shadow-sm">
                <h2 class="text-2xl font-semibold text-gray-800 mb-6">Shipping Information</h2>
                <form class="space-y-4">
                    <div>
                        <label for="fullName" class="block text-gray-700 font-medium mb-2">Full Name</label>
                        <input type="text" id="fullName" name="fullName" placeholder="John Doe" class="form-input" required>
                    </div>
                    <div>
                        <label for="address" class="block text-gray-700 font-medium mb-2">Address</label>
                        <input type="text" id="address" name="address" placeholder="123 Main St" class="form-input" required>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div>
                            <label for="city" class="block text-gray-700 font-medium mb-2">City</label>
                            <input type="text" id="city" name="city" placeholder="Anytown" class="form-input" required>
                        </div>
                        <div>
                            <label for="state" class="block text-gray-700 font-medium mb-2">State / Province</label>
                            <input type="text" id="state" name="state" placeholder="State" class="form-input" required>
                        </div>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div>
                            <label for="zipCode" class="block text-gray-700 font-medium mb-2">Zip Code</label>
                            <input type="text" id="zipCode" name="zipCode" placeholder="12345" class="form-input" required>
                        </div>
                        <div>
                            <label for="country" class="block text-gray-700 font-medium mb-2">Country</label>
                            <input type="text" id="country" name="country" placeholder="United States" class="form-input" required>
                        </div>
                    </div>
                    <div>
                        <label for="phone" class="block text-gray-700 font-medium mb-2">Phone Number</label>
                        <input type="tel" id="phone" name="phone" placeholder="(123) 456-7890" class="form-input">
                    </div>
                </form>
            </div>

            <!-- Order Summary & Payment -->
            <div class="lg:col-span-1 bg-gray-50 p-6 rounded-lg shadow-sm">
                <h2 class="text-2xl font-semibold text-gray-800 mb-6">Order Summary</h2>
                <div class="space-y-2 mb-6 text-gray-700">
                    <div class="flex justify-between">
                        <span>Subtotal:</span>
                        <span>$1,299.99</span>
                    </div>
                    <div class="flex justify-between">
                        <span>Shipping:</span>
                        <span>$50.00</span>
                    </div>
                    <div class="flex justify-between font-bold text-lg border-t pt-2 mt-2">
                        <span>Total:</span>
                        <span>$1,349.99</span>
                    </div>
                </div>

                <h2 class="text-2xl font-semibold text-gray-800 mb-6">Payment Information</h2>
                <form class="space-y-4">
                    <div>
                        <label for="cardNumber" class="block text-gray-700 font-medium mb-2">Card Number</label>
                        <input type="text" id="cardNumber" name="cardNumber" placeholder="**** **** **** ****" class="form-input" required>
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label for="expiryDate" class="block text-gray-700 font-medium mb-2">Expiry Date (MM/YY)</label>
                            <input type="text" id="expiryDate" name="expiryDate" placeholder="12/25" class="form-input" required>
                        </div>
                        <div>
                            <label for="cvv" class="block text-gray-700 font-medium mb-2">CVV</label>
                            <input type="text" id="cvv" name="cvv" placeholder="123" class="form-input" required>
                        </div>
                    </div>
                </form>

                <button class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-full shadow-lg transition duration-300 transform hover:scale-105 mt-8">
                    Place Order
                </button>
            </div>
        </div>

        <!-- Back to Cart/Shop Link -->
        <div class="mt-8 text-center">
            <a href="detail.html" class="text-indigo-600 hover:text-indigo-800 font-medium flex items-center justify-center">
                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
                Back to Product Details
            </a>
        </div>
    </section>

    <!-- Footer Section -->
    <footer class="bg-gray-800 text-white py-8 rounded-t-lg mt-8">
        <div class="container mx-auto text-center px-4">
            <p>&copy; 2025 FurniCo. All rights reserved.</p>
            <div class="flex justify-center space-x-4 mt-4">
                <a href="#" class="text-gray-400 hover:text-white transition duration-300">Privacy Policy</a>
                <a href="#" class="text-gray-400 hover:text-white transition duration-300">Terms of Service</a>
            </div>
        </div>
    </footer>
</body>
</html>
